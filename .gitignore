node_modules/
.env
.env.local
.vercel
*.log
.DS_Store

# local Claude Code settings (personal, may contain tokens in allow-list entries)
.claude/settings.local.json

# large binary build output / stale duplicate copies — not meant to live in git
mobile/public/*.apk
frontend/
verify.demo/

# private backup of operational details trimmed from the public docs
# (deployment gotchas, Upstash setup, secrets handling) — local reference only
INTERNAL-NOTES.md
